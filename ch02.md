# ch02 错题 — 基础数据类型与运算符

## [ch02] 练习 6 — split/join 方向写反

- **状态**：✅ 已改正（2026-09-01）
- **题目**：把 `"2026-09-01"` 转成 `"2026/09/01"`

### 错误代码

```python
def reformat_date(date_str):
    return date_str.split("-").join("/")
```

### 报错信息

```
AttributeError: 'list' object has no attribute 'join'
```

### 错因分析

把两个方法的方向记反了：

- `split` 是**字符串**的方法：字符串 → 列表
- `join` 是**分隔符字符串**的方法：列表 → 字符串

`split("-")` 返回的是列表 `['2026', '09', '01']`，而 `join` 不是列表的方法，
所以链式调用直接 `AttributeError`。

### 正确写法

```python
def reformat_date(date_str):
    return "/".join(date_str.split("-"))
#       └ 分隔符在前面 ┘└─ 被切的字符串 ─┘
```

读法：先把 `date_str` 按 `-` 切成列表，再用 `/` 把列表拼起来。

### 一句话教训

> **split 是字符串的方法，join 是分隔符的方法**——谁的方法谁调用。

---
